/*
function openAndClose(url, width, height, closeDelay) {
  let userWindow = window.open(url, "_blank", `location = yes, height=${height}, width=${width}, scrollbars=yes, status=yes`)
  setTimeout(() => {
    userWindow.close()
  }, closeDelay * 1000)
}
openAndClose("https://www.youtube.com/", 500, 800, 2)

*/

// ** 0 **